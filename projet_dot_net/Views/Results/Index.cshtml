@model IEnumerable<projet_dot_net.Model.Results>

@{
    ViewBag.Title = "Index";
}

<header class="jumbotron-fluid">
    <br />
    <br />
    <br />
    <br />
    <h2 class="text-center text-primary">Liste des résultats</h2>
</header>


<section class="container">

    <p>
        @Html.ActionLink("Ajouter un résultat", "Create", null, new { @class = "btn btn-outline-dark pull-right" })
    </p>
    <br />
    <table class="table">
        <thead class="thead-dark">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Note)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Evaluations.Id)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Pupils.FirstName)
                </th>
                <th></th>
            </tr>
            </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Note)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Evaluations.Id)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Pupils.FirstName)
                    </td>
                    <td>
                        @Html.ActionLink("Modifier", "Edit", new { id = item.Id }) |
                        @Html.ActionLink("Voir détails", "Details", new { id = item.Id }) |
                        @Html.ActionLink("Delete", "Supprimer", new { id = item.Id })
                    </td>
                </tr>
            }
        </tbody>
    </table>
</section>